package com.kaustavn.algorithms;

public class MakeAnagrams {

	/*
	 * Given two strings (lowercase), how many characters do we need to
	 * remove(from either) to make them anagrams.
	 * 
	 * eg: hello, billion ---> answer 6 (he from hello and biin from billion
	 */
}
